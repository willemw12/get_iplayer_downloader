# Maintainer: willemw12 <willemw12@gmail.com>

#CHECK gnome dependencies

pkgname=get_iplayer_downloader-git
pkgver=20120104
pkgrel=1
pkgdesc="A GUI download utility for BBC get_iplayer"
arch=('any')
url="https://github.com/willemw12/get_iplayer_downloader"
license=('GPL3')
#depends=('get_iplayer' 'gtk3' 'python2>=2.7')
depends=('gtk3' 'python2>=2.7')
makedepends=('git')
optdepends=('get_iplayer' 'get_iplayer-git')
provides=('get_iplayer_downloader')
conflicts=('get_iplayer_downloader')
source=(https://github.com/willemw12/get_iplayer_downloader/tarball/master)
md5sums=('d07c0b4ddbf23700b8a811ee24d170b0')

_gitroot=git://github.com/willemw12/get_iplayer_downloader.git
_gitname=$pkgname

build() {
  cd "$srcdir"
  msg "Connecting to GIT server...."

  if [[ -d "$_gitname" ]]; then
    cd "$_gitname" && git pull origin
    msg "The local files are updated."
  else
    git clone "$_gitroot" "$_gitname" --depth=1
  fi

  msg "GIT checkout done or server timeout"
}

package() {
  cd "$srcdir/$pkgname"
  #--prefix=/usr
  python2 setup.py install --root="$pkgdir/" --optimize=1 || return 1
}

# vim:set ts=2 sw=2 et:

